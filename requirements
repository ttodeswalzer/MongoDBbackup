# Damise Admin Panel v2.0 - Gerekli Modüller
# Python 3.8+ gereklidir

# Temel HTTP ve API işlemleri
requests>=2.28.0

# SSH tünelleme
sshtunnel>=0.4.0

# Şifreleme (config güvenliği için)
cryptography>=3.4.8

# MongoDB bağlantısı ve işlemleri
pymongo>=4.0.0

# BSON (MongoDB veri formatı)
# pymongo ile birlikte gelir, ayrı yükleme gerekmez

# Kurulum komutu:
# pip install -r requirements.txt

# Alternatif tek komut:
# pip install requests sshtunnel cryptography pymongo